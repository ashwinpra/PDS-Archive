#include<stdio.h>
int main(int argc, char const *argv[])
{
    //used to change the type of variable
    float b = 53.2;
    float a = (float)57/5;
    printf("The value is %d and %f",(int)b,a);
    return 0;
}
