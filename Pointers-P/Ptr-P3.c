#include<stdio.h>
#include<string.h>
int checkPalindrome(char[], int, int);//string, starting index, ending index

int main() {
    //returns length of largest palindromic substring
    printf("Enter string:");
    char str[100];
    scanf("%s",str);

    for(int i=0;i<strlen(str);i++){
        
    }
    return 0;
}
