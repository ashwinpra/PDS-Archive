#include<stdio.h>
#include<math.h>

typedef struct{
    float x,y;
}pt;

float dist(pt, pt);
//returns farthest two points among array of points
void checkFarthest(pt []);


int main() {
   

    return 0;
}
